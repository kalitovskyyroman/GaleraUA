import leoLogo from '../../assets/images/logo/leo_logo.png';
import codes from '../../config/codes';

const config = {
    text: 'Not found',
    code: codes.NotFound,
    logo: leoLogo,
};

export default config;
